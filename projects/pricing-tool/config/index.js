// Main config file that exports all project configurations
const ai = require("./ai");
// Future configs can be added here:
// const features = require('./features');
// const integrations = require('./integrations');

module.exports = {
  ai,
  // Export other configs as you add them:
  // features,
  // integrations
};
